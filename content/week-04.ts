import { type WeekEntry } from './schema';

/**
 * Week 4 of the programme.
 *
 * **Filled by F9.8.** `10-content-pipeline.md` requires one week at a
 * time with the validator run and the counts reported after each — batching
 * four weeks into one pass gets truncated, and truncated content fails
 * silently: 900 words and a passing build.
 */
export const WEEK_04: WeekEntry = {
  weekIndex: 4,
  words: [],
  sentenceItems: [],
  days: [],
};
